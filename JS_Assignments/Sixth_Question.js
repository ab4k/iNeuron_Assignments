// 06. 'Facebook, Google, Microsoft, Apple, IBM, Oracle, Amazon' split the string at the comma and change it to an array.

let givenString = "Facebook, Google, Microsoft, Apple, IBM, Oracle, Amazon";
let resultedArray = givenString.split(",");
console.log(resultedArray);
